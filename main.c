#include <stdio.h>

main() {
    
   /*insert menu w 6 assessment options */
   
   
   
   /*TASK 1: ROTATION CIPHER: encrypt message provided cipher text and rotation amount */
 
 printf("Enter a text message for encryption: \n");
 scanf(%d, &/*store message in declared variable*/);
 printf("Enter a key: \n");
 scanf(%d, &/*store key in declared variable*/);  // message will be encrypted using alphabetical shift of input integer
  
  /* perform encryption */
 
 printf("Encrypted message: \n", & /*store encrypted message in variable*/ );

  return 0;
}
